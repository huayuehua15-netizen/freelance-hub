# Freelance Hub 开发进度与交接文档

> 更新时间：2026-08-15（二次交接）｜ 工作区：`C:\dev\freelance_hub\`

---

## 一、项目总览

三端架构，离线优先 + 增量同步：

| 端 | 技术栈 | 目录 | 状态 |
|---|---|---|---|
| 移动端 | Flutter 3.47 + Hive + Provider | `mobile/` | P0 核心链路完成，已部署真机 |
| 后端 | Node 20 + Express + MongoDB(Mongoose) + JWT | `backend/` | 代码完整，依赖已装，**未验证启动** |
| Web 后台 | Vue3 + Vite + ECharts + Element Plus | `web/` | 代码 90% 完成，依赖未装 |

文档依据：`C:\Users\niu\Downloads\Freelance Hub 全量详细开发文档（V2.0）.md`

---

## 二、各端完成度

### 移动端 `mobile/`（P0 全链路已通，已上真机）

**已完成**：
- 5 个 Hive 模型（user_info/client_project/time_log/expense_log/tax_category）+ app_config
- 计时器状态机（idle/running/paused + 开始/暂停/继续/结束/取消 + 杀进程异常恢复 recoverTimer）
- 计时器**前台常驻通知**（start/pause/resume 显示，stop/cancel 清除）
- 项目 CRUD + 软删 + 批量归档/删除 + Free 版 3 项目上限拦截
- 开支 CRUD + 税务类目 + 可抵扣标记 + 收据拍照
- 数据看板（本月工时/收入/开支 + 最近记录）
- 三档订阅页（Free/Monthly/Annual + Demo 沙盒切换 + 试用警告）
- 月度/年度报表（fl_chart 图表 + 本地 PDF 导出 + 15.3% 自雇税 + 免责声明）
- 设置页（货币/时区/语言/JSON 导出/清除缓存真实实现）
- 深色模式、i18n 基础（en/zh）、订阅到期提醒
- **预置 Demo 数据**（3 项目 + 12 工时 + 4 开支，对齐文档 §10）
- **后端对接**：登录/注册/登出/会话恢复、401 自动刷新、增量同步 push/pull

**真机部署**：新包已装到 Redmi（包名 `com.freelancehub.freelance_hub`），`pm clear` 后首启已自动 seed Demo 数据。

### 后端 `backend/`（代码完整，未验证启动）

**已完成**（零 TODO）：
- 鉴权：register/login/refresh/me/logout/deleteAccount（bcrypt + JWT + refreshToken 轮换）
- 同步：SyncService（LWW 冲突的 batchUpsert + 增量 pullSince）
- 7 控制器、7 路由、4 Mongoose 模型、auth/premium/rateLimit/errorHandler 中间件
- 报表：月度 + 年度聚合（**本次补齐** byProject/byCategory/monthlyTrend）
- PDF 导出（pdfkit）
- **本次改动**：非生产环境 register 默认 `premiumType=annual`（+1年有效期），便于演示同步/Web

**配置**：
- `backend/.env` 已填入 Atlas URI（`MONGODB_URI=mongodb+srv://huayuehua15_db_user:***@cluster0.iopwtul.mongodb.net/freelance_hub?...`）
- 依赖已装：`node_modules` 共 185 个包（`npm install --no-package-lock` 成功）

**⚠️ 未验证启动**（本沙箱环境限制）：
1. Atlas 连接被拒：`querySrv ECONNREFUSED _mongodb._tcp.cluster0.iopwtul.mongodb.net`（沙箱网络限制，或 Atlas 集群未激活 / Network Access 未加 IP 白名单）
2. 端口 3000 被本机另一服务占用（curl 返回中文「未登录或登录已失效」，非本后端响应）

### Web `web/`（代码 90%，依赖未装）

**已完成**：
- 登录页、路由守卫、MainLayout、API 层（axios + 401 自动刷新）
- **本次修复**：`App.vue` 从渲染 HelloWorld 改为 `<router-view>`（关键 bug，此前路由失效）
- **本次修复**：DashboardView/AnnualReportView/ExportView 从 mock 改为接真实 API
- 删除 HelloWorld.vue 残留

**未完成**：依赖未装（`web/node_modules` 不存在，需 `npm install`）

---

## 三、本次会话改动清单（2026-08-14）

### 移动端 P0 补缺（19:36）
| 文件 | 改动 |
|---|---|
| `mobile/lib/services/demo_data_seeder.dart` | **新增**：seedIfEmpty 写入 3项目+12工时+4开支 |
| `mobile/lib/app.dart` | 调用 seeder + 注册 SyncService + premium 联动 |
| `mobile/lib/providers/project_provider.dart` | createProject 加 currency 形参 |
| `mobile/lib/screens/projects_screen.dart` | 传 CurrencyFormat.current |
| `mobile/lib/screens/settings_screen.dart` | 清除缓存真实实现 + 手动同步接 syncAll |
| `mobile/lib/services/notification_service.dart` | 计时器常驻通知 3 方法 |
| `mobile/lib/providers/timelog_provider.dart` | start/pause/resume/stop 接通知 |
| `mobile/test/widget_test.dart` | 修复 MyApp 报错 → CurrencyFormat 测试 |

### 核心同步链路补齐（20:30）
| 文件 | 改动 |
|---|---|
| `backend/src/services/reportService.js` | getAnnualReport 补 monthlyTrend/byProject/byCategory |
| `backend/src/controllers/authController.js` | dev 环境 register 默认 annual |
| `backend/.env` | MONGODB_URI 改为 Atlas 连接串 |
| `web/src/App.vue` | HelloWorld → router-view |
| `web/src/views/DashboardView.vue` | 接 reportApi，4 图表 |
| `web/src/views/AnnualReportView.vue` | 接 API + 季度表 |
| `web/src/views/ExportView.vue` | 接 reportApi.exportPdf |
| `mobile/lib/models/user_info.dart` | 加 toJson/fromJson |
| `mobile/lib/providers/auth_provider.dart` | 接 ApiService 实现登录/注册/登出/会话恢复 |
| `mobile/lib/services/api_service.dart` | 401 refreshToken 刷新重试 |
| `mobile/lib/services/sync_service.dart` | 实现 push/pull + _toJson + _fromServer |
| `mobile/lib/screens/login_screen.dart` | 登录后联动 premium |

---

## 四、关键坑与技术细节（务必先读）

1. **包名是 `com.freelancehub.freelance_hub`**（不是文档写的 `com.freelancehub.app`）。`pm clear`/`uninstall` 必须用前者。
2. **端口 3000 被占**：本机另一服务占用 3000。后端需改 `.env` 的 `PORT=3001`（或查清占用），并同步改移动端 `API_BASE_URL` 和 Web `VITE_API_BASE_URL`。
3. **权限是两套系统**：移动端 `PremiumProvider`（本地 Demo 模拟）与后端 DB `user.premiumType` 独立。同步接口 `/project|timelog|expense/batch-upsert|pull` 需后端 `requireAnnual`（读 DB）。本次已让 dev 注册默认 annual + 移动端登录后联动 premiumProvider。
4. **移动端真机访问后端**：不能用模拟器的 `10.0.2.2`，需传局域网 IP：
   `flutter run --dart-define=API_BASE_URL=http://<电脑局域网IP>:3001/api/v1`
5. **字段映射**：后端模型用 timestamps 生成 `serverCreateTime/serverUpdateTime`(Date→ISO 字符串)，无移动端的 createdAt/updatedAt/syncStatus。同步时 `clientUpdatedAt`=本地 updatedAt；pull 时 serverUpdateTime ISO→int。
6. **沙箱限制**：WorkBuddy 沙箱无法构建 Android APK（gradle EPERM）、无法连外网（Atlas DNS 被拒）。后端 npm install 需 `--no-package-lock`（写 package-lock.json 会 EPERM）。
7. **adb**：`C:\Users\niu\AppData\Local\Android\Sdk\platform-tools\adb.exe`，PowerShell 调用需 `&` 前缀。WiFi ADB：`192.168.1.101:5555`。

---

## 五、待完成事项

### 阻塞项（需在用户本机环境做）
1. **验证后端启动**：本沙箱无法连 Atlas。用户需在自己电脑跑 `cd backend && node src/app.js`，确认 Atlas 连通 + 服务监听。
   - 若 Atlas 仍连不上：检查 Atlas 集群是否已创建/激活、Network Access 是否加了 `0.0.0.0/0`、用户名密码是否正确。
2. **解决端口冲突**：改 `backend/.env` 的 `PORT=3001`。
3. **Web 装依赖**：`cd web && npm install`。

### 端到端联调（核心同步链路验证）
1. 启动后端（`node src/app.js`，端口 3001）
2. 启动 Web（`cd web && npm run dev`，默认 5173，配 `VITE_API_BASE_URL=http://localhost:3001/api/v1`）
3. 移动端重新构建（传 `--dart-define=API_BASE_URL=http://<电脑IP>:3001/api/v1`）并装真机
4. 移动端注册/登录 → 设置页「Cloud Sync」手动同步 → 数据上云
5. Web 用同一账号登录 → 大屏看到同步数据

### 未实现的 P2 功能（不影响核心演示）
- RevenueCat 真实订阅接入（移动端 + 后端 webhook）
- 后端报表 PDF 批量导出（CSV）
- i18n 全量迁移（仅底部导航+Dashboard 已迁）
- workmanager 前台服务保活（当前靠绝对时间戳+recoverTimer 保证杀进程不丢）

---

## 六、快速启动命令（在用户自己的 PowerShell 终端）

```powershell
# 后端（先改 .env 的 PORT=3001）
cd C:\dev\freelance_hub\backend
npm install          # 已装过可跳过
node src/app.js      # 看到 "MongoDB Connected" + "running on port 3001" 即成功

# Web（新终端）
cd C:\dev\freelance_hub\web
npm install
$env:VITE_API_BASE_URL="http://localhost:3001/api/v1"
npm run dev          # http://localhost:5173

# 移动端（新终端，传局域网 IP）
cd C:\dev\freelance_hub\mobile
flutter build apk --debug --dart-define=API_BASE_URL=http://192.168.1.xxx:3001/api/v1
& "C:\Users\niu\AppData\Local\Android\Sdk\platform-tools\adb.exe" -s 192.168.1.101:5555 install -r build\app\outputs\flutter-apk\app-debug.apk
```

---

## 七、当前阻塞总结

后端代码、依赖、Atlas 配置**全部就绪**，唯一卡点是**本沙箱无法验证启动**（外网 DNS 被拒 + 端口冲突）。换到用户本机环境，按上面命令跑一次 `node src/app.js` 即可确认后端可用，随后即可端到端联调。

---

## 八、二次交接改动（2026-08-15 bug 修复批次）

### 本次修复的 bug 清单

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 1 | `backend/src/app.js` | errorHandler 在 404 catch-all **之后**注册，导致 404 被当错误处理 | 移除冗余 404 catch-all（Express 默认 404 已足够），errorHandler 作为最后中间件 |
| 2 | `mobile/lib/services/sync_service.dart` | `_pushTimeLogs` 发送字段名 `'timelogs'`（小写 l），后端期望 `'timeLogs'`（大写 L），**工时同步永远失败** | 改为 `'timeLogs'` |
| 3 | `backend/src/services/syncService.js` | `pullSince` 游标分页用 `$gt since + $lt cursor`，等于 cursor 的记录被漏掉 | 改为 `$gte since + $lt cursor`，边界正确 |
| 4 | `backend/src/controllers/timelogController.js` | `list` 方法 `startTime` 用展开运算符合并，FREE 版限制会覆盖用户参数，且多次合并产生脏查询 | 先组装 `startTimeRange` 对象再赋值，逻辑清晰 |
| 5 | `backend/src/controllers/expenseController.js` | 同 #4，`expenseDate` 同样的合并 bug | 同 #4 修法 |
| 6 | `backend/src/controllers/reportController.js` | `exportPdf` 的 `doc.end()` 异步发送响应，但 controller 已同步返回；PDF 生成异常无法被 `next(error)` 捕获 | 用 Promise 封装 PDF 生成（`buildPdf`），`await` 后再 `res.send`，异常走 `next` |
| 7 | `backend/src/services/syncService.js` | `batchUpsert` 的 `Object.assign(existing, record)` 把 `clientUpdatedAt`/`deviceId` 等非 schema 字段写入 document | 先解构排除 `clientUpdatedAt, deviceId`，再 `Object.assign` |
| 8 | `backend/.env` | `PORT=3000` 与本机其他服务冲突 | 改为 `PORT=3001` |
| 9 | `backend/.env` | `MONGODB_URI` 用 SRV 格式（`mongodb+srv://`），TXT 记录解析超时（`queryTxt ETIMEOUT`） | 改为标准格式（显式列出 3 个 shard 主机 + ssl/replicaSet/authSource 参数） |
| 10 | `mobile/lib/config/app_config.dart` | 默认 `apiBaseUrl` 端口 3000 | 改为 3001 |
| 11 | `web/src/api/request.js` | 默认 `baseURL` 端口 3000 | 改为 3001 |

### 验证结果

- **Web 构建**：`npm run build` 成功（2221 模块，23s），仅有 chunk 大小警告（非阻塞）
- **Web 依赖**：已安装（133 包）
- **后端 lint**：无错误
- **后端 Atlas 连接**：TCP 连通性已确认（`Test-NetConnection` 到 shard-00-00:27017 成功），DNS SRV 解析正常；标准连接串已配置，但 **shell 环境死锁无法完成启动验证**（node 进程阻塞了唯一 shell 会话）

### Atlas 连接串变更说明（重要）

原始 SRV 格式：
```
mongodb+srv://user:pass@cluster0.iopwtul.mongodb.net/freelance_hub?retryWrites=true&w=majority
```
该格式需要 DNS 解析 SRV + TXT 记录，本机网络环境 TXT 解析超时。

已改为标准格式（当前 .env 内容）：
```
mongodb://user:pass@ac-udzagxb-shard-00-00.iopwtul.mongodb.net:27017,ac-udzagxb-shard-00-01.iopwtul.mongodb.net:27017,ac-udzagxb-shard-00-02.iopwtul.mongodb.net:27017/freelance_hub?ssl=true&replicaSet=atlas-udzagxb-shard-0&authSource=admin&retryWrites=true&w=majority
```
此格式直接指定所有 shard 节点，跳过 SRV/TXT 解析。

**如果标准格式仍连不上**，检查：
1. Atlas Dashboard → Network Access 是否添加了 `0.0.0.0/0`（允许所有 IP）
2. Database Access 的用户名密码是否正确
3. `replicaSet` 名称是否为 `atlas-udzagxb-shard-0`（在 Atlas Cluster → Connect → Connect your application 可确认）

### 用户需做的下一步

1. **在 PowerShell 跑**：`cd C:\dev\freelance_hub\backend; node src/app.js`
   - 看到 `MongoDB Connected` + `running on port 3001` 即成功
   - 若 Atlas 仍超时，按上面「Atlas 连接串变更说明」排查
2. **启动 Web**：`cd C:\dev\freelance_hub\web; $env:VITE_API_BASE_URL="http://localhost:3001/api/v1"; npm run dev`
3. **端到端验证**：Web 注册/登录 → 移动端同账号登录 → 设置页手动同步 → Web Dashboard 看到数据

---

## 九、三次交接改动（2026-08-15 检查+补全批次）

### 本次审查范围

逐行检查后端全部 27 个文件 + Web 全部 13 个文件 + 移动端同步/认证/权限/种子数据关键链路。此前二次交接的修复（errorHandler 顺序、timeLogs 大小写、$gte 边界、batchUpsert 字段排除、PORT=3001）均已确认在代码中生效。

### 本次修复/新增清单

| # | 文件 | 问题 | 修复 |
|---|---|---|---|
| 1 | `backend/src/config/database.js` | `serverSelectionTimeoutMS: 5000` 太短，Atlas 首连（TCP+TLS+认证）在弱网/VPN 下误判失败 | 改为 30000 |
| 2 | `backend/src/app.js` | CORS `origin` 仅允许 `localhost:5173`，局域网访问 Web（手机演示）会被浏览器拦截 | dev 环境放开（`origin: true`），仅 prod 收紧 |
| 3 | `backend/src/controllers/authController.js` | 软删账号（isDeleted=true）的邮箱仍被 unique 索引占用，无法重新注册 | register 检测到 isDeleted 旧账号 → 复活（重置密码/恢复活跃/保留业务数据），返回 200 |
| 4 | `backend/src/controllers/reportController.js` + `routes/report.js` | 缺 Web 批量导出 CSV | 新增 `GET /report/export-csv?year=`（Annual 专属，RFC4180 转义 + BOM），输出工时+开支两段 |
| 5 | `web/src/api/index.js` + `views/ExportView.vue` | ExportView 只有 PDF | 新增「Export CSV」按钮 + `reportApi.exportCsv` |
| 6 | `mobile/lib/services/sync_service.dart` | pull 时若 `serverTs > local.updatedAt` 无条件覆盖本地，可能吞掉本地未推送修改（syncStatus=0） | 三处 `_pull*` 改为：本地 syncStatus=0 时不覆盖、标记 syncStatus=2（冲突保留本地）；否则才覆盖 |

### 本次审查确认无问题的关键链路（勿再动）

- 同步字段映射：`timeLogs`（大写 L）、`clientUpdatedAt`、`serverUpdateTime` ISO→int 解析 —— 全对
- LWW 冲突：删除永远优先 + `clientTs >= serverTs` 覆盖 —— 逻辑正确
- JWT：access/refresh 双 secret 隔离、refresh 轮换（服务端校验旧 token 失效）
- 移动端 401 → refreshToken 刷新重试（排除 `/auth/refresh` 防死循环）
- Web axios 401 → refresh 重试，blob 响应不被解包破坏
- 权限：后端 `requireMonthly/requireAnnual`、Web 守卫 `premiumType==='annual'`、移动端 `applyServerEntitlement` 三端一致
- Demo 数据：41.5h / $2110.00 / $219.97 对齐文档 §10，syncStatus=0（待同步）保证首次同步上云
- 移动端登录链路：login/register → identifyUser → applyServerEntitlement → dashboard

### 验证结果（本会话）

- 后端全部改动文件 `node --check` 通过
- Web `npm run build` 成功（48s，仅 chunk 大小警告）

### 待用户处理（本会话环境无法代办）

1. **Atlas IP 白名单（当前唯一硬阻塞）**：用户开了 VPN，Atlas 拒绝其出口 IP。解决（二选一）：
   - Atlas 网页 → Network Access → Add IP Address → `0.0.0.0/0`（开发期推荐，允许所有 IP）
   - 或查当前 VPN 出口 IP（如浏览器访问 whatismyip.com）加入白名单
2. **无线调试**：手机 WiFi ADB 连接已断开。拿到配对码+IP:PORT 后执行：
   `adb pair IP:PORT 配对码` → `adb connect IP:PORT`（PowerShell 中 `& "C:\Users\niu\AppData\Local\Android\Sdk\platform-tools\adb.exe"`）
3. **启动联调**（Atlas 通了以后）：
   ```
   cd C:\dev\freelance_hub\backend; node src/app.js        # 端口 3001
   cd C:\dev\freelance_hub\web; $env:VITE_API_BASE_URL="http://localhost:3001/api/v1"; npm run dev
   ```
4. **移动端重建**（改 sync 逻辑后需重新装真机，API 指向电脑局域网 IP）：
   ```
   cd C:\dev\freelance_hub\mobile
   flutter build apk --debug --dart-define=API_BASE_URL=http://<电脑IP>:3001/api/v1
   然后 update_phone.bat（或手动 adb install）
   ```

### 已知限制（不阻塞演示）

- deleteAccount 的「30 天后物理删除」无后台定时任务（复活机制已兜底邮箱占用）
- RevenueCat 真实订阅未接（webhook 已就绪，需配置 RC 项目 + Play 内购）
- 后端报表按 UTC 日期聚合（dailyHoursTrend 用 toISOString，与用户本地时区可能差一天边缘）

---

## 十、四次交接改动（2026-08-15 MongoDB 修复 + Git 里程碑）

### 一、MongoDB 连接最终修复（决定性）

**现象**：用户开放 Atlas 白名单（0.0.0.0/0）后仍连不上，`node src/app.js` 报 `Server selection timed out`。

**排查链**（每步都有实测证据）：

| 步骤 | 测试 | 结果 |
|---|---|---|
| 1 | 三个 shard 主机 TCP 27017 连通性 | 全部 TCP_OK |
| 2 | Mongoose 多主机 + `replicaSet=atlas-udzagxb-shard-0` | `ReplicaSetNoPrimary`，servers 为空 |
| 3 | 单主机 `directConnection=true` | 连接成功但写操作报 **`not primary`** |
| 4 | 对每个 shard 执行 `hello` 命令 | **真相：副本集名是 `atlas-7jqenn-shard-0`（不是 .env 里写的 `atlas-udzagxb-shard-0`）**；primary 是 shard-00-02 |

**根因**：`.env` 连接串的 `replicaSet` 参数值与 Atlas 实际副本集名不匹配，驱动无法加入副本集 → 无法发现 primary → 所有写操作失败。

**修复**：`backend/.env` 的 MONGODB_URI 改为正确的副本集名：

```
mongodb://huayuehua15_db_user:密码@ac-udzagxb-shard-00-00.iopwtul.mongodb.net:27017,ac-udzagxb-shard-00-01.iopwtul.mongodb.net:27017,ac-udzagxb-shard-00-02.iopwtul.mongodb.net:27017/freelance_hub?ssl=true&replicaSet=atlas-7jqenn-shard-0&authSource=admin&retryWrites=true&w=majority
```

**验证（全部通过）**：
- ✅ `node src/app.js` → `MongoDB Connected` + `running on port 3001`
- ✅ `GET /api/v1/health` → `{"status":"ok"}`
- ✅ 注册接口（写操作）→ `REGISTER_OK: premiumType=annual`（不再 not primary）
- 后端当前以 `Start-Process` 后台运行，日志在 `backend/server.out.log`

**⚠️ 若未来副本集名变化**：对每个 shard 直连执行 `db.admin().command({hello:1})` 读 `setName` 字段即可（已在排查链验证此法有效）。

### 二、Git 仓库里程碑（首次提交并推送）

- **远程**：`origin → https://github.com/huayuehua15-netizen/freelance-hub.git`
- **提交**：`ab7dce7`（root-commit）「Initial commit: Flutter mobile app + Express/MongoDB backend + Vue3 web dashboard」—— 133 文件 / 15913 行
- **分支**：`main`，已设置上游跟踪 `origin/main`，本地与远程同步
- **敏感文件检查**：`.env`（含 MongoDB 密码）、`node_modules`、`build/`、日志均未提交（三层 .gitignore 生效）
- **注意**：Windows Git Credential Manager 之前有旧凭据指向 `193.112.220.241:3000`（个人 Git 代理），github.com 的认证最终由 GCM 弹窗完成（push 曾因 `-NoNewWindow` 隐藏弹窗而卡住，前台方式即成功）

### 三、待办状态更新

- ~~Atlas IP 白名单~~ ✅ 已解决（用户开放 0.0.0.0/0 + 副本集名修正）
- ~~Git 初始化提交推送~~ ✅ 完成
- 无线调试：等用户提供配对码 + IP:PORT（adb pair → connect）
- 端到端联调：后端已在 3001 运行，待无线重连后装新 APK（API_BASE_URL=电脑局域网IP:3001）
